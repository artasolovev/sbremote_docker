sudo docker volume create sbremote_data

sudo docker build -t sbremote_docker .

sudo docker run --rm -it \
--network=host \
-v sbremote_data:/data \
sbremote_docker \
--setup

sudo docker run -d \
--name SBRemote \
--restart=unless-stopped \
--network=host \
-v sbremote_data:/data \
sbremote_docker \
--run